/*
  Порожній прошарок — див. коментар у app/transactions/layout.tsx.
  Тут був згенерований RootLayout із вкладеними <html>/<body>.
  Файл можна видалити.
*/
export default function ApiConsoleLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
